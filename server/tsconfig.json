{
  "extends": "../tsconfig.base.json",

  "compilerOptions": {
    /* Language and Environment */
    "lib": ["ES2024"],

    /* Modules */
    "types": ["node", "express"],

    /* Emit */
    "outDir": "dist"
  },

  "include": ["src/**/*.mts"]
}